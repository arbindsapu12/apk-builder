name: Build APK

on:
  workflow_dispatch:
  push:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Create dummy APK
        run: |
          echo "fake apk content" > app.apk
      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: my-apk
          path: app.apk
